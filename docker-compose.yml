version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:8080"
    environment:
      - MONGO_URI=${MONGO_URI}
      - PORT=${PORT}
    restart: unless-stopped
